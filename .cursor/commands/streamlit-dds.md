# Streamlit DDS dashboard

Read and implement **entire** specification:

@.cursor/prompts/streamlit-dds-dashboard.md

Also: @campaigns/eftihia-sepolia-001/deploy.json and all campaigns/*/deploy.json

**First message for user:** use full block from `.cursor/README.md` § «Дашборд ДДС — первое сообщение» (slash may not work).

Start Phase A+B. Sepolia only. Etherscan-first data. Parent/child DDS rows. Page 5 balances.

Do not create new Merkle campaigns. Do not commit `.env`.
