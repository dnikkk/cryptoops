# Пример для Streamlit Community Cloud → Settings → Secrets
# Скопируйте в UI (без PRIVATE_KEY — дашборду не нужен).

ETHERSCAN_API_KEY = "ваш_ключ_etherscan"
SEPOLIA_RPC_URL = "https://ethereum-sepolia-rpc.publicnode.com"
